<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.drozee.drozeebvest">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logow"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logow"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity" />
        <activity android:name=".PreferencesActivity" />
        <activity android:name=".IDLogin" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".Thanks" />
        <activity android:name=".Forgot" />
        <activity android:name=".SignUpMessage" />
        <activity android:name=".SignUp">

        </activity>
        <activity android:name=".IDLoginup" />
        <activity android:name=".DetailsSignup" />
        <activity android:name=".Book" />
        <activity
            android:name=".MainActivitN"
            android:label="@string/title_activity_main_activit_n"
            android:theme="@style/AppTheme" />
        <activity android:name=".LoginUpdated" />
        <activity android:name=".ChoosePlan" />
        <activity android:name=".Splash"> <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
    </application>

</manifest>